import 'package:flutter/material.dart';

class TopAppBarRight extends StatelessWidget {
  const TopAppBarRight({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [SizedBox(width: 300, child: Text("A"))],
    );
  }
}
